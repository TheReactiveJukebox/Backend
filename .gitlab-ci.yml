stages:
- build_backend
- build_image

variables:
  IMAGE_DEV_TAG: backend_dev:$CI_COMMIT_REF_NAME

build_backend:
  stage: build_backend
  image: dockette/mvn
  script:
    - mvn --batch-mode --quiet --fail-at-end compiler:compile war:war
  artifacts:
    paths:
    - target/server.war

build_image_dev:
  stage: build_image
  only:
    - master
    - ft/gitlab_ci
  script:
    - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN 129.217.62.109:4790
    - docker build --pull -t $IMAGE_DEV_TAG -f Dockerfile.dev .
    - docker push $IMAGE_DEV_TAG
